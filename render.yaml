services:
  - type: web
    name: sawari-backend
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_VERSION
        value: 18.0.0
      - fromGroup: sawari-secrets
